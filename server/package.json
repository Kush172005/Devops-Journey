{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/",
    "test:ci": "mkdir -p test-reports/coverage && c8 --reports-dir=test-reports/coverage --reporter=lcov --reporter=text --reporter=json-summary --check-coverage=false -- node --test test/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "c8": "^12.0.0",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1"
  }
}